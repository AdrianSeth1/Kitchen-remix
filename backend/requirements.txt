# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Validation
pydantic>=2.7.0

# Image handling
Pillow>=10.3.0

# ML — install BEFORE diffusers, match your CUDA version
# torch==2.3.1+cu121  # uncomment and pin to your CUDA; install via pip index URL
# torchvision==0.18.1+cu121

# Diffusion pipeline (verify class names against docs before Phase 1)
diffusers>=0.30.0
transformers>=4.41.0
accelerate>=0.30.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Optional: faster fp8 kernels
# optimum>=1.20.0
# quanto>=0.2.0

# Dev
python-dotenv>=1.0.0
