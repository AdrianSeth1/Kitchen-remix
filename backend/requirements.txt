# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Validation
pydantic>=2.7.0

# Image handling
Pillow>=10.3.0

# ML — install BEFORE diffusers, match your CUDA version
# torch==2.3.1+cu121  # uncomment and pin to your CUDA; install via pip index URL
# torchvision==0.18.1+cu121

# Diffusion pipeline
# FluxKontextPipeline lives in diffusers main branch only (not yet on PyPI stable).
# Install with:  pip install git+https://github.com/huggingface/diffusers.git
# Once a stable release ships this can be pinned to a version number.
diffusers @ git+https://github.com/huggingface/diffusers.git
transformers>=4.44.0
accelerate>=0.30.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# fp8 quantization of the transformer — cuts VRAM ~50 % (≈12 GB vs ≈24 GB) so it
# fits a 24 GB card with headroom. Enabled by default (config.USE_FP8); set USE_FP8=0
# to disable. diffusers uses optimum-quanto under the hood for the float8 path.
optimum-quanto>=0.2.4

# Dev
python-dotenv>=1.0.0
